# railway.toml
# Railway 部署配置文件
# 把 backend 文件夹上传到 Railway 后，它会自动读取这个文件来启动服务

[build]
builder = "nixpacks"

[deploy]
startCommand = "uvicorn main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
